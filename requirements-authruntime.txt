# Dependencies for the ADR-007 auth/runtime adapter (app/authruntime/*).
# Install with: pip install -r requirements-authruntime.txt
# (also declared as the "authruntime" extra in pyproject.toml)
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
authlib>=1.3,<2
itsdangerous>=2.2,<3
python-multipart>=0.0.9,<1
httpx>=0.27,<1
jinja2>=3.1,<4
