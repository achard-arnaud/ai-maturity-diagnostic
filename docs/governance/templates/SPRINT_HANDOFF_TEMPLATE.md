# STOP — Epic {NN} / Sprint S{NN}

## Objective

<Copy verbatim from the Epic's sprint plan row.>

## State

- Sprint branch: `<branch>`, forked from `<base>` (commit `<sha>`).
- Base branch for the PR: `<dev|other>`.
- <Any other state a reader needs before touching this again.>

## Outputs

<Bullet list of files/artifacts this Sprint added or changed, one line each,
with a one-sentence "why".>

## Evidence

<What you actually ran and observed — real gate/test output, not a
description of what should happen. Copy exact PASS/FAIL lines or counts.>

## Remaining (deferred, not blocking this Sprint's stop condition)

<What later Sprints/Epics still own, so nobody re-discovers it from scratch.>

## Commands to resume

```bash
<exact commands to get back to a working state>
```

## Risks / notes

<Anything a reviewer or the next Sprint needs to know that isn't obvious
from the diff — judgment calls, known-fragile assumptions, things you
decided not to do and why.>
